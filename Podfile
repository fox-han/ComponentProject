# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'ComponentProject' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for ComponentProject
  pod 'HSAFNetworking', :path => 'Lib/HSAFNetworking'
  pod 'HSBaseModel', :path => 'Lib/HSBaseModel'
  pod 'HSLogining', :path => 'Lib/HSLogining'
  
  target 'ComponentProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ComponentProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
